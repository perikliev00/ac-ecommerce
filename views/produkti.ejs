<!DOCTYPE html>
<%
  const locale = typeof currentLocale !== 'undefined' && currentLocale ? currentLocale : 'bg';
  const title = locale === 'en' ? 'All products' : locale === 'de' ? 'Alle Produkte' : locale === 'ru' ? 'Все товары' : 'Всички продукти';
%>
<html lang="<%= typeof htmlLang !== 'undefined' && htmlLang ? htmlLang : 'bg' %>">
<head>
  <%- include('partials/head', { extraStyles: ['hyperinverter.css'] }) %>
</head>
<body>
  <%- include('partials/loader') %>
  <%- include('partials/header') %>
  <%- include('partials/drawer') %>

  <main class="page" role="main">
    <%- include('partials/catalog', { catalogTitle: title, breadcrumbCurrent: title }) %>
  </main>

  <%- include('partials/footer', { extraScripts: ['produkti.js'] }) %>
</body>
</html>
