<!DOCTYPE html>
<html lang="bg">
<head>
  <%- include('partials/head', { title: typeof title !== 'undefined' ? title : 'Грешка – Несебър Клима', description: typeof description !== 'undefined' ? description : 'Страницата не е намерена или възникна грешка.' }) %>
  <style>
    /* Sticky footer: main content grows so footer sits at bottom of viewport */
    body.error-layout { display: flex; flex-direction: column; min-height: 100vh; }
    body.error-layout .page { flex: 1; display: flex; align-items: center; justify-content: center; }
    .error-page { text-align: center; padding: 3rem 1rem; max-width: 36rem; margin: 0 auto; }
    .error-page__code { font-size: 4rem; font-weight: 700; color: #1a365d; line-height: 1; margin-bottom: 0.5rem; }
    .error-page__title { font-size: 1.5rem; margin-bottom: 1rem; color: #2d3748; }
    .error-page__text { color: #4a5568; margin-bottom: 2rem; line-height: 1.6; }
    .error-page__link { display: inline-block; padding: 0.75rem 1.5rem; background: #1a365d; color: #fff; text-decoration: none; border-radius: 0.25rem; font-weight: 600; }
    .error-page__link:hover { background: #2c5282; }
  </style>
</head>
<body class="error-layout">
  <%- include('partials/loader') %>
  <%- include('partials/header') %>
  <%- include('partials/drawer') %>

  <main class="page" role="main">
    <section class="error-page" aria-label="Грешка">
      <div class="error-page__code"><%= typeof statusCode !== 'undefined' ? statusCode : 500 %></div>
      <h1 class="error-page__title"><%= typeof is404 !== 'undefined' && is404 ? 'Страницата не е намерена' : 'Възникна грешка' %></h1>
      <p class="error-page__text"><%= typeof message !== 'undefined' && message ? message : 'Моля, опитайте отново по-късно или се върнете към началната страница.' %></p>
      <a class="error-page__link" href="/">Към началната страница</a>
    </section>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
